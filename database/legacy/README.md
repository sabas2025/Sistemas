# SQLs legados

Estes arquivos foram movidos para evitar execução acidental.

Para instalação nova use somente:

- `database/install.sql`
- `database/install_final_v104_7.sql`

Para banco existente use no painel:

- `Central Técnica > Validar Banco`

O reparo idempotente fica em `app/Services/DatabaseSchemaGuardService.php`.
